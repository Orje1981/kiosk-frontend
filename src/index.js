import React from "react";
import ReactDOM from "react-dom";
import KioskControlDashboard from "./KioskControlDashboard";

ReactDOM.render(
    <React.StrictMode>
        <KioskControlDashboard />
    </React.StrictMode>,
    document.getElementById("root")
);
